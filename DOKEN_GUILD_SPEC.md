# DOKENギルド 確定仕様

最終整理: 2026-08-31 / v198

この文書は、DOKENギルドの変更で過去仕様が戻る回帰を防ぐための基準です。`main` の一時的な状態より、ここに記載した確定仕様とユーザーが後から明示した要望を優先します。

## 投稿

- 一般利用者の新規投稿は `guild.html` 内の `submitPost()` を正規処理とする。拡張JSは通常投稿を独自実装しない。
- 投稿目的、カテゴリ、急募、作業日、作業時間、職種、人数、条件、地域、投稿者名、非公開電話番号、掲載期限、写真、本文、下書きを維持する。
- 電話番号は一般画面・公開投稿本文・通知メールに表示しない。支部管理者のみ確認し、支部が問い合わせを取り次ぐ。
- 作業日のある人材投稿はDOKENギルド専用カレンダーの対象とする。

## 掲載期限

選択肢は次をすべて維持する。

- 24時間
- 48時間
- 3日
- 7日（初期値）
- 14日
- 1か月
- 2か月
- 3か月
- 6か月
- 1年
- 作業日まで
- 無期限

`1m/2m/3m/6m/1y` はカレンダー月・年加算で期限を計算する。`unlimited` は `expiresAt` を空にし、期限切れ確認の対象にしない。既存の長期期限データを `legacy` に落とさない。

## 投稿本文

- 入力した改行・空行・段落を掲示板でも保持する。
- CRLF/LFは統一する。
- 3行以上連続する過剰な空行のみ2行へ整理してよい。
- 表示CSSは `white-space: pre-wrap` を維持する。

## カテゴリーUI

「何を探しますか？」は以下の4つ。

- すべて
- 人材・仕事
- 資材・道具
- 相談・情報

「すべてすべて」のような重複表示は禁止。選択時は形状を四角く変えず、色反転で示す。すべて=青、人材=オレンジ、資材=緑、相談=紫を基本とし、ライト/ダーク双方で判別可能にする。

## 管理者

- 管理者認証後だけ管理操作を表示・実行する。
- 現在投稿だけでなく、期限切れ・解決済み・過去投稿も管理対象とする。
- 投稿の編集は `prompt()` の連続入力ではなく、新規投稿と同じフォームを開き既存値を入れて行う。
- 編集対象: 投稿目的、カテゴリ、急募、本文、地域、作業日、作業時間、職種、人数、条件、掲載期限、写真、投稿者情報。
- 非公開電話番号は管理者APIから取得して編集フォームへ入れる。取得できない場合は編集を開始しない。
- 完全削除は期限確認（7日継続/解決済み）とは別操作。削除前に復元不可を明示して確認し、管理者PIN付きクラウド `delete` を実行する。
- 期限切れ投稿は7日再掲載できる。

## 回帰防止

変更時は最低限、次を確認する。

1. 新規投稿の正規 `submitPost()` が残っている。
2. 長期掲載期限6種類がすべて存在する。
3. `unlimited` が期限切れにならない。
4. 投稿本文の改行が正規化で空白化されない。
5. 「すべて」の補助文が重複表示されない。
6. 管理者編集は通常投稿と同じフォームを使う。
7. 完全削除は `delete` APIを使用し、`reviewExpiry` と混同しない。
8. 電話番号を公開しない。
9. ライト/ダーク双方の選択色を維持する。
10. DOKENギルド専用カレンダー、コメント、検索、問い合わせを変更で壊さない。

関連履歴: PR #109, #110, #111, #112, #113, #114。