# DOKENギルド 確定仕様

最終整理: 2026-08-31 / v199

この文書は、DOKENギルドの変更で過去仕様が戻る回帰を防ぐ基準です。`main` の一時的な状態より、ここに記載した確定仕様と、その後ユーザーが明示した要望を優先します。

## 正規実装と停止済み実装

- `guild.html`: 新規投稿、本文正規化、掲載期限、期限切れ・過去判定、検索、カレンダー、下書きの正規実装。
- `assets/js/guild-canonical-v199.js`: 管理者の編集・完全削除・再掲載だけを追加する保護レイヤー。通常投稿は必ず `guild.html` の `submitPost()` へ委譲する。
- `assets/css/guild-v195.css`: カテゴリー選択色と管理者編集モードだけを担当する。本文の段落表示やカテゴリー名を生成しない。
- `assets/css/theme-polish.css`: カテゴリーのレイアウトとライト・ダーク表示を担当する。HTMLの実テキストを隠さず、疑似要素で名称を複製しない。
- `docs/guild-apps-script-template.js`: クラウド保存、通知、管理者API、公開データからの電話番号除外の正規バックエンド。
- `guild-config-v194.js`、`guild-v195-loader.js`、`guild-admin-editor-v195.js`、`guild-display-hotfix-v197.js`、`guild-canonical-v198.js` は停止済み。`guild-config.js` とService Workerから再読込しない。

## v199で確定した回帰差分

| 機能 | v199直前のmain | 過去の最終仕様 | v199基準 |
|---|---|---|---|
| 本文段落 | 表示側で復元を試みたが、旧クラウド保存時に改行が空白化 | 改行・空行・段落を保持 | 入力、送信、保存、読込、表示の全段階で保持 |
| 掲載期限 | 画面拡張は長期、クラウドは14日までで長期を7日に変換 | 12選択肢、初期7日 | 画面・計算・クラウドを同一定義に統一 |
| 過去判定 | 有効な長期期限でも作成30日後に過去扱い | 有効期限まで掲載 | 30日判定は期限情報のない旧投稿だけ |
| カテゴリー | HTML、共通JS、2系統のCSS疑似要素が別名称を生成 | 確定4名称を一度だけ表示 | HTMLの実テキストだけを名称とする |
| 再掲載 | 終了ワークフローが残り、7日継続後も過去扱いになる場合あり | 募集中として7日再掲載 | 終了ワークフローを未対応へ戻して7日再掲載 |
| 管理画面 | 現在・過去の管理切替が拡張整理時に消失 | 現在/過去/要確認/全投稿を管理 | 管理ダッシュボードへ復元 |
| 下書き | 管理者編集の入力で一般下書きを上書きする可能性 | 一般下書きを保持 | 管理者編集時は下書き保存を停止し、終了時にフォームを初期化 |

## 投稿

- 一般利用者の新規投稿は `guild.html` 内の `submitPost()` を正規処理とする。管理者拡張は編集時以外、この関数へ委譲する。
- 投稿目的、カテゴリ自動設定、急募、作業日、作業時間、職種、人数、条件、地域、投稿者名、非公開電話番号、掲載期限、写真、文章自動作成、本文、下書き保存・復元を維持する。
- クラウド投稿に成功するまでフォームと下書きを消さない。成功後だけフォームを初期値へ戻す。
- 新規投稿通知先はブラウザの値を信用せず、Apps Scriptの `NOTIFY_EMAIL` Script Propertyだけを使う。
- 作業日のある人材投稿だけをDOKENギルド専用カレンダーへ表示する。過去・終了投稿は表示しない。

## 投稿本文と段落

- 入力した改行、空行、段落を掲示板と通知文で保持する。
- CRLF/LFをLFへ統一する。行末・行頭の不要な空白を除き、3個以上連続する改行だけ2個へ整理する。
- 表示CSSは `guild.html` の `.post-body { white-space: pre-wrap; }` を正規定義とする。
- Apps Scriptは本文だけ `safeMultilineText_()` を使う。単行用 `safeText_()` を本文へ使ってはならない。
- 旧バックエンド互換として、クライアントは改行をU+2028で送信し、読込時にLFへ戻す。更新済みバックエンドもU+2028をLFへ戻すため、新旧どちらでも段落を失わない。

## 掲載期限

選択肢と順序は次の12個。初期値は7日だけとする。

1. 24時間
2. 48時間
3. 3日
4. 7日（初期値）
5. 14日
6. 1か月
7. 2か月
8. 3か月
9. 6か月
10. 1年
11. 作業日まで
12. 無期限

- `1m/2m/3m/6m/1y` はカレンダー月加算とし、1月31日から1か月は2月末、うるう日から1年は翌年2月末へ丸める。
- `workdate` は作業日の日本時間23:59:59。未入力・不正・過去日なら安全な既定値7日へ戻す。
- `unlimited` は `expiresAt` を空にし、期限切れ確認と30日互換判定の対象にしない。
- 旧バックエンドが長期期限を7日へ丸める期間でも、画面へ表示しない `clientId` 内の `__dx:` 互換トークンで元の選択肢と日時を復元する。新バックエンドは読込時にこれを正規データへ移行する。

## 期限切れ・過去投稿

判定順は次の通り。

1. `status=closed` または `workflow=matched/failed/completed` は過去・解決済み。
2. 無期限は自動で過去へ移さない。
3. `expiresAt` が過去なら期限切れ確認対象かつ過去表示。
4. 作業日を過ぎ、`lastReviewedAt` が作業日終了後でなければ確認対象。
5. 有効な将来の `expiresAt` があれば、作成から30日を過ぎても掲載を続ける。
6. `expiresAt` がない旧データだけ、作成30日後を互換的に過去扱いとする。

「7日継続」「再掲載」は `reviewExpiry(continue)` を使い、`lastReviewedAt` を更新し、`status=open`、`expiryOption=7d`、期限を現在から7日後にする。終了済みワークフローは先に `unhandled` へ戻す。解決済みは `status=closed`、`workflow=completed` とする。

## カテゴリーUI

「何を探しますか？」の実テキストは次の4つだけ。

- すべて
- 人材・仕事
- 資材・道具
- 相談・情報

共通JavaScriptで名称を書き換えたり、CSSの `content` で同じ名称を生成したりしない。「すべてすべて」を含む重複表示を禁止する。選択時は角丸形状を保ったまま、すべて=青、人材=オレンジ、資材=緑、相談=紫で示し、ライト/ダーク双方で白文字とのコントラストを確保する。

## 管理者

- 管理者認証後だけ管理操作を表示・実行する。
- 現在掲載、過去・終了、要確認、全投稿の件数と、現在/過去の管理切替を表示する。
- 投稿編集は新規投稿と同じフォームへ既存値を入れて行う。連続 `prompt()` 編集へ戻さない。
- 編集対象は目的、カテゴリ、急募、本文、地域、作業日、作業時間、職種、人数、条件、掲載期限、写真、投稿者名、非公開電話番号。
- 非公開電話番号を管理者APIで取得できない場合は編集を開始しない。
- 管理者編集中は一般利用者の端末下書きを変更しない。編集の保存・中止後は編集値をフォームに残さない。
- 完全削除は期限確認とは別操作とし、復元不可を明示して確認後、管理者PIN付き `delete` APIを実行する。
- 投稿者連絡先確認、交渉中、解決済み、7日再掲載、取り次ぎ状況、LINE文コピー、期限切れ確認を維持する。

## 電話番号と問い合わせ

- 電話番号は一般画面、公開 `list` API、タグ、本文、コメント、LINE告知文へ出さない。
- `publicPost_()` は必ず `tel` を削除する。端末保存用公開データも `tel` を削除する。
- 管理者だけ `getContact` をPIN付きで利用できる。
- 一般利用者の問い合わせ先は埼玉土建 上尾伊奈支部（048-773-9863）で、支部が投稿者へ取り次ぐ。

## UI/PWA

- iPhone縦幅430px以下ではカテゴリーを1列にし、文字を省略しない。タップ領域は最低44pxを保つ。
- 固定投稿ボタンは下部ナビと `safe-area-inset-bottom` を避ける。
- 文字拡大を禁止しない。ライト/ダーク双方で本文、カテゴリー、管理操作を読める配色にする。

## 必須回帰チェック

`node scripts/guild-regression-check.js` は少なくとも次を挙動で検証する。

- 段落正規化、旧クラウド互換送信・復元、表示CSS
- 12個の期限選択肢、初期7日、月末・うるう日計算、無期限
- 期限切れ、将来の長期期限、旧データ30日互換、再掲載
- 確定カテゴリー名と疑似要素・共通JSによる重複がないこと
- 通常投稿への委譲、管理者API、下書き分離、現在/過去管理
- Apps Scriptの段落保存、長期期限、電話番号非公開、終了ワークフロー解除
- Service Workerがv199だけをキャッシュし、停止済み旧スクリプトを再読込しないこと

関連履歴: PR #109, #110, #111, #112, #113, #114, #115。変更時は `node scripts/quality-check.js`、`node scripts/guild-regression-check.js`、`git diff --check` をすべて通す。
