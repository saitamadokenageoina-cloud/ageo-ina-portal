# Home 3D artwork

This directory is reserved for independent cinematic 3D artwork assets. The current recovery release deliberately uses the verified per-card artwork under `assets/illustrations/home/` instead of the corrupted atlas so cards never render blank. New 3D assets must be added here before CSS references them.
